/**
 * EmitEventConstants
 */
export class EmitEventConstants {
  /** 数据源URL、Token更新 */
  static readonly SOURCE_URL_TOKEN_UPDATED: number = 10001;

  /** 当前歌曲更新 */
  static readonly CURRENT_SONG_UPDATED: number = 10002;

  /** 睡眠定时器状态变化（设置/取消/到期） */
  static readonly SLEEP_TIMER_STATE_CHANGED: number = 10003;
}
